# A$AP Fotki by A$AP Docker

## Usage

```
python asap.py file1 file2 [--out out_file]
```

Unfortunately, task A does not support CLI yet, so you should run it manually:
```
python NN/b.py
```
specifying the template photo and search pool photos manually.


2019 (c) Ivan, Kirill, Artem, Volodya
